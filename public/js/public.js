/**
 * Optional front-end helpers for the [happybites] shortcode.
 *
 * The guest QR menu is a separate PWA and does not use this file.
 * Credit / "powered by" markup must never be injected into the host theme.
 *
 * @package HappyBites
 */
(function () {
    'use strict';
})();
